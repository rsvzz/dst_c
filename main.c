#include <stdio.h>
#include <stdlib.h>
//#include "headers/Stack.h"
#include "headers/sqstc.h"

int main(){
    /*
    Queue* qe = malloc(sizeof(Queue));
    add(1, qe);
    add(2, qe);
    add(3, qe);
    add(4, qe);
    
    while (qe->front != NULL)
    {
        printf("%d ,", qe->front->key);
        del_queue(qe);
    }
    */
    return 0;
}